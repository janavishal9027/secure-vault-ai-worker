# Local dev config (gitignored). Summaries run on Gemini via its
# OpenAI-compatible endpoint — the AsyncOpenAI client just points at the
# Gemini base URL with the Gemini key + a Gemini model.
GEMINI_API_KEY=${GEMINI_API_KEY}
GEMINI_MODEL=${GEMINI_MODEL}
APP_HOST=0.0.0.0
APP_PORT=8000
